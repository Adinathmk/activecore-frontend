{
  "routes": [
    { "handle": "filesystem" },

    {
      "src": "^/api/(.*)",
      "dest": "https://activecore.duckdns.org/api/$1"
    },

    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}